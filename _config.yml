kitle: "Basile Pajot"
title: "Basile Pajot"
description: "Personal site generated from README.md"
baseurl: ""
url: ""
markdown: kramdown
kramdown:
  input: GFM
sass:
  style: compressed
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules

# Use local layouts/includes from this repository (custom theme in `_theme`)
# We removed the external `theme:` gem to use local `_layouts` and `_includes`.
